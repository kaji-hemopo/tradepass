[railway]
  builder = "dockerfile"
  dockerfile = "Dockerfile"

[deploy]
  num_replicas = 1
  restart = "yes"
  timeout = 60

[deploy.startCommand]
  command = "streamlit run app.py --server.port 8501 --server.address 0.0.0.0"

[environment]
  TRADEPASS_API_URL = "$TRADEPASS_API_URL"
